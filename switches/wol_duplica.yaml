# https://home-assistant.io/components/switch.wake_on_lan/
  - platform: wake_on_lan
    name: WOL_duplica
    mac: !secret duplica_mac
    host: !secret duplica_ip
    turn_off:
      service: hassio.addon_stdin
      data:
        addon: core_rpc_shutdown
        input: !secret duplica_host
